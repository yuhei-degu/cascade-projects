-- =============================================================
-- 🚫 DEPRECATED（2026-04-18 破棄）
-- =============================================================
-- 当初 `lunaria_core_memory` に `superseded_by_profile` フラグと
-- 部分インデックス、lunaria_mark_superseded RPC を追加する案だったが、
-- Phase A の実測で既存 `memory_category='profile'` マーカーが既に
-- 存在することが判明したため不要になった。
--
-- 新プランでは：
--   - プロンプト注入時に memory_category='profile' を除外するだけ
--   - 既存データ 2 件は cleanup_profile_duplicates.sql で手動クリーンアップ
--   - 抽出パイプラインの振り分けで今後の流入を防ぐ
--
-- 新プランは PROFILE_MEMORY_INTEGRATION.md v2 を参照。
-- このファイルは流さないこと。
-- =============================================================
